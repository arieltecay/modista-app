import React from 'react';

/**
 * Línea de costura punteada (basting stitch) — motivo de marca "Atelier Digital".
 * Divider entre secciones de la landing v2.
 */
const StitchDivider: React.FC = () => <hr className="stitch-divider my-2" aria-hidden="true" />;

export default StitchDivider;
